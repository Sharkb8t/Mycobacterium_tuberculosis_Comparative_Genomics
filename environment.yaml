name: mtb_comp_genomics
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.9
  # Core packages
  - biopython=1.81
  - numpy=1.24.0
  - pandas=2.0.0
  - matplotlib=3.7.0
  - seaborn=0.13.2
  - scipy=1.10.0
  - scikit-learn=1.2.0
  - jupyter=1.0.0
  - notebook=6.5.0
  
  # Bioinformatics packages (Windows-compatible from conda)
  - pysam=0.21.0
  - pyfaidx=0.7.0
  
  # Visualization
  - bokeh=3.0.0
  - plotly=5.14.0
  
  # Utilities
  - tqdm=4.65.0
  - requests=2.31.0
  - beautifulsoup4=4.12.0
  
  # Bioinformatic tools (optional)
  - mummer4=4.0.0
  - blast=2.13.0
  - samtools=1.17
  - bcftools=1.17
  
  # Pip packages (if needed)
  - pip
  - pip:
    # Any packages only available via pip